#soal no 4

#a
x = 2
df = 10
dchisq(x , df)

#b
#untuk running nya langsung di blok saja yang 4.a dan 4.b
p <- rchisq(100,df = 10)

hist(p,
     freq = FALSE,
     xlim = c(0,30),
     ylim = c(0,0.2),
     main = "Histogram Of Chisquare")

#c
#mean
df

#variance
2*df
